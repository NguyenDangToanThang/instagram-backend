
spring:
  application:
    name: instagram-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/instagram
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8080
  servlet:
    context-path: /api/v1